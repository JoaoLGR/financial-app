export function resolveInvoiceForPurchase(purchaseDate: string, closingDay: number, dueDay: number) { const purchase = new Date(`${purchaseDate}T12:00:00`); const closeMonth = new Date(purchase.getFullYear(), purchase.getMonth() + (purchase.getDate() > closingDay ? 1 : 0), 1); const lastClose = new Date(closeMonth.getFullYear(), closeMonth.getMonth() + 1, 0).getDate(); const close = new Date(closeMonth.getFullYear(), closeMonth.getMonth(), Math.min(closingDay, lastClose)); const dueMonth = dueDay > close.getDate() ? closeMonth : new Date(closeMonth.getFullYear(), closeMonth.getMonth() + 1, 1); const lastDue = new Date(dueMonth.getFullYear(), dueMonth.getMonth() + 1, 0).getDate(); return { referenceMonth: `${closeMonth.getFullYear()}-${String(closeMonth.getMonth()+1).padStart(2,'0')}-01`, closingDate: close.toISOString().slice(0,10), dueDate: new Date(dueMonth.getFullYear(),dueMonth.getMonth(),Math.min(dueDay,lastDue)).toISOString().slice(0,10) }; }
