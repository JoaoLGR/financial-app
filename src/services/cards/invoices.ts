import type { SupabaseClient } from '@supabase/supabase-js';
export async function createCreditCardExpense(client: SupabaseClient, input: { workspaceId:string; cardId:string; categoryId:string; description:string; amount:number; purchaseDate:string; notes?:string }) { return client.rpc('create_credit_card_expense', { p_workspace_id:input.workspaceId, p_credit_card_id:input.cardId, p_category_id:input.categoryId, p_description:input.description, p_amount:input.amount, p_purchase_date:input.purchaseDate, p_notes:input.notes || null }); }
export async function payInvoice(client: SupabaseClient, input: { workspaceId:string; invoiceId:string; accountId:string; paymentDate:string }) { return client.rpc('pay_credit_card_invoice', { p_workspace_id:input.workspaceId, p_invoice_id:input.invoiceId, p_account_id:input.accountId, p_payment_date:input.paymentDate }); }
